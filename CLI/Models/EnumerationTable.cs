namespace UML.Models
{
	public enum Commands
	{
		exit,
		add_class,
		add_attr,
		add_meth,
		rem_class,
		rem_attr,
		rem_meth,
		relat,
		rem_relat,
		save,
		load,
		import,
		export,
		help
	}
}
