namespace CLI.Models
{
    public enum Commands
    {
        exit,
        add_class,
        add_field,
        add_meth,
        rem_class,
        rem_field,
        rem_meth,
        relat,
        rem_relat,
        save,
        load,
        import,
        export,
        help
    }
}